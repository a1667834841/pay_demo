server:
  port: 9002

spring:
  profiles:
    active: dev
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  datasource:
    username: hsp
    password: 123456
    url: jdbc:sqlite:data/pay.db
    driver-class-name: org.sqlite.JDBC
    tomcat:
      max-active: 1
  #    schema: classpath:schema.sql
#    data: classpath:data.sql
#    initialization-mode: always
#    continue-on-error: true
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update


logging:
  level:
    root: info



